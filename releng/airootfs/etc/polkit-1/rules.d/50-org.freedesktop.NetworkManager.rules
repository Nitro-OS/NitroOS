/* Allow members of the wheel group or the nitroos user to manage network connections without password */
polkit.addRule(function(action, subject) {
    if (action.id.indexOf("org.freedesktop.NetworkManager.") === 0 &&
        (subject.user === "nitroos" || subject.isInGroup("wheel"))) {
        return polkit.Result.YES;
    }
});
